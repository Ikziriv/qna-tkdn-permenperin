# Production environment variables (client-side)
# Vite requires client env vars to be prefixed with VITE_
# This URL points to the deployed Railway backend
VITE_API_URL=https://qna-tkdn.up.railway.app/api

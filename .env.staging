# Staging environment variables (client-side)
VITE_API_URL=/api

# Server-only env vars (set via Railway dashboard for staging)
# DATABASE_URL=
# JWT_SECRET=
# NODE_ENV=staging
# PORT=4000
# CORS_ORIGIN=https://staging-your-app.up.railway.app
